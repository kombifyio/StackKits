# kombify.me Integration

`kombify.me` is the managed-domain mode for StackKits. In public examples it is
treated as an external service contract: the CLI can register a subdomain prefix
when the user provides the required API credential, and generated URLs use the
assigned prefix.

Public releases must not include internal gateway hostnames, private IP
addresses, deployment notes, or operator credentials.
