# Prompt: Inspect Existing Rollout

Inspect an existing StackKits workspace without mutation.

Run:

```bash
stackkit status --json
stackkit verify --http --json
stackkit logs list --json
stackkit doctor --json
```

Report current StackKit and mode, Hub URL, service URLs, failing checks, latest run ID, evidence paths, and whether generated rollout files appear to have been edited manually.

